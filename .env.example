# Bring-your-own-key for the optional VLM-as-policy path (Chapter 36). The offline core does not
# need any of these. Copy to `.env` and fill in only the provider you want to use.

# Anthropic (Claude) — a high-level planner / policy over the toy env.
ANTHROPIC_API_KEY=

# OpenAI (GPT) — alternative provider for the same path.
OPENAI_API_KEY=
